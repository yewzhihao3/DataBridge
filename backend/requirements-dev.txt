# Development dependencies.
# Install with: pip install -r requirements-dev.txt
# This file includes requirements.txt so you only need one install command.

-r requirements.txt

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=8.2.0

# pytest-asyncio: allows writing async def test_... functions.
# asyncio_mode = "auto" in pyproject.toml means you don't need the
# @pytest.mark.asyncio decorator on every async test.
pytest-asyncio>=0.23.0

# httpx: async HTTP client used by FastAPI's TestClient.
# Lets you send real HTTP requests to the app in tests without a live server.
httpx>=0.27.0
